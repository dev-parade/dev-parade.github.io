name: PosiDev Monitor - メンション監視 & ポジデブ自動返信

on:
  schedule:
    - cron: '0 */3 * * *'  # 3時間ごとにメンション監視
  workflow_dispatch:

permissions:
  issues: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install tweepy

      - name: Monitor mentions & auto-reply
        env:
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
        run: python scripts/debu-posi-monitor.py

      - name: Create Issue with report
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f monitor_issue.md ]; then
            DATE=$(date +%Y-%m-%d_%H:%M)
            gh issue create \
              --title "🍖 [ポジデブ変換] ${DATE}" \
              --body "$(cat monitor_issue.md)" \
              --label "debu-posi-reply" || \
            gh issue create \
              --title "🍖 [ポジデブ変換] ${DATE}" \
              --body "$(cat monitor_issue.md)"
          fi
