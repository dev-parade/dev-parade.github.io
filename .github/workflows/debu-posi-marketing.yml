name: PosiDev Daily Tweet - 毎日ポジデブ自動投稿

on:
  workflow_dispatch:
    inputs:
      campaign:
        description: 'キャンペーン名 (launch / scheduled / collab)'
        required: false
        default: 'scheduled'
  schedule:
    - cron: '0 3 * * *'  # 毎日 12:00 JST に自動投稿

permissions:
  issues: write

jobs:
  daily-tweet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install tweepy

      - name: Generate and auto-post daily tweet
        env:
          CAMPAIGN: ${{ github.event.inputs.campaign || 'scheduled' }}
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_SECRET: ${{ secrets.X_ACCESS_SECRET }}
        run: python scripts/debu-posi-tweet-generator.py

      - name: Create Issue with tweet
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -f tweet_issue.md ]; then
            DATE=$(date +%Y-%m-%d)
            gh issue create \
              --title "🍖 [ポジデブ日報] ${DATE}" \
              --body "$(cat tweet_issue.md)" \
              --label "tweet-ready" || \
            gh issue create \
              --title "🍖 [ポジデブ日報] ${DATE}" \
              --body "$(cat tweet_issue.md)"
          fi
